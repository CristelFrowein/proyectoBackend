
<h1>Carrito de Compras</h1>
{{#if cart.products.length}}
    <ul>
        {{#each cart.products}}
            <li>{{this.productId.title}} - ${{this.productId.price}} (Cantidad: {{this.quantity}})</li>
        {{/each}}
    </ul>
{{else}}
    <p>Tu carrito está vacío.</p>
{{/if}}
